# API Reference

## Pay product webhook replay

`POST /v1/merchant/webhooks/{deliveryId}/retry` is an authenticated owner/developer operation for terminal `dead_letter` deliveries only. The JSON body is `{ "reason": "...", "idempotencyKey": "..." }`. A successful request creates a separately signed delivery with a new delivery ID, `parentDeliveryId`, replay actor and reason, then attempts delivery. Pending/retrying records remain controlled by exponential backoff; paid state is never affected.

## Pay invoice v3 tip

`POST /v1/merchant/invoices` accepts optional integer `tipAmount` alongside the integer base `amount`. The tip must be between zero and the base amount. The response uses invoice version 3 with `baseAmount`, `tipAmount`, and `amount = baseAmount + tipAmount`; all three values and the fee breakdown are covered by the merchant Ed25519 signature. Central Pay intent/invoice creation uses the total amount.

## Pay recurring draft

`POST /v1/merchant/recurring-drafts` accepts `payer`, `description`, integer `amount`, `cadenceDays`, `maximumOccurrences`, RFC3339 `startsAt`, and `idempotencyKey` for an owner/finance session. It returns a merchant-signed status `draft` record with `automaticChargeEnabled=false` and `walletApprovalEveryOccurrence=true`. This endpoint never creates a payment, invoice, standing authorization, or paid state.

Core: `GET /health`, `GET /status`, `GET /node/identity`, `GET /metrics`, `GET /blocks/latest`, `GET /txs/{hash}`, `GET /accounts/{address}`, `GET /validators`, `GET /validators/peers`, `GET /validators/peer-sync`, `POST /validators/{address}/heartbeat`, `POST /validators/{address}/peers/observe`, `POST /validators/{address}/peer-sync`.

Account inputs accept canonical 20-byte EVM `0x...` addresses and their checksummed Bech32 `ynx1...` aliases where wallet accounts are used by account, Faucet, transfer, stake, resource, and Trust-trace routes. The server normalizes both representations to lowercase `0x...` before state lookup or mutation. This does not change EVM JSON-RPC, which continues to require EVM addresses. See `docs/ecosystem/ADDRESS_FORMATS.md`.

`GET /status` includes `validatorCount`, `readyValidatorCount`, `validatorPeerReadiness`, `validatorPeerDiscovery`, `validatorPeerSync`, `nodeIdentity`, `replication`, `catchingUp`, and `build` so node operators and public proof checks can distinguish configured validators, locally ready validators, expected bootstrap peers, observed peers, recorded sync evidence, the configured local validator identity, peer-sync freshness, current follower replication convergence, and the non-secret binary release identity serving the endpoint.

`GET /node/identity` returns non-secret runtime identity evidence for the local node: `validatorAddress`, validator moniker/role/host/peer ID when configured, `expectedValidatorCount`, `peerSyncTargetCount`, target validator addresses, `peerSyncInterval`, `build`, `peerSyncFreshness`, and `replication`. `build` contains `commit`, `release`, and `buildTime`; local development defaults are `unknown`, `local`, and `unknown`, while deploy builds inject `ynx-chaind` release identity through Go linker flags. Freshness records classify each configured target as fresh, stale, missing, synced, or lagging using persisted `/validators/peer-sync` records. Follower `replication` reports `starting`, `syncing`, `synced`, `catching_up`, `stale`, `degraded`, or `stopped`, plus source/local height and hash, height lag, freshness, attempts, successes, total and consecutive failures, bounded last-error/stage evidence, and synchronization timestamps. `catchingUp` remains true until an authenticated snapshot has been reverified after process start. The replication runtime record itself is node-local and intentionally resets on restart; the chain snapshot remains persistent. This endpoint is designed for post-deploy proof and does not expose private keys, replication keys, secrets, or full env files.

`GET /metrics` exposes bounded authoritative replication telemetry without source URLs, keys, or error text: `ynx_chain_replication_configured`, `ynx_chain_replication_status_info`, `ynx_chain_replication_catching_up`, `ynx_chain_replication_fresh`, `ynx_chain_replication_lag_blocks`, process-local attempts/successes/failures counters, consecutive failures, and the last-success timestamp. The status label is limited to the runtime lifecycle enum. Each deployed follower must be scraped as a distinct target before these metrics or their alerts count as remote fault-detection evidence.

Deploy bundles also include `config/release-manifest.json` with SHA-256 checksums for binaries, role env files, and service config. Public proof expects `/status.build` and `/node/identity.build` to match that manifest's `commit` and `release`, and remote SSH verification compares the installed `ynx-chaind` checksum to the manifest before treating the node as release-identifiable.

`GET /validators` returns `validators` plus `expectedValidatorCount`. Validator records include `address`, `moniker`, optional `host`, optional `role`, optional `peerId`, `votingPower`, `active`, `peerReady`, optional `peerStatus`, optional `latestHeight`, optional `lastSeenAt`, optional `updatedAt`, and optional `peerEvidence`. Production/testnet nodes load configured validator identity from `YNX_VALIDATOR_SET`; if it is missing, the node exposes only the local default validator and remote public proof must fail multi-validator checks. Runtime peer readiness is persisted in the local chain snapshot and survives restart for matching validator addresses.

`POST /validators/{address}/heartbeat` records local validator peer-readiness evidence for an existing validator. Request body accepts optional `peerId`, optional `host`, optional `ready`, optional `status`, optional `latestHeight`, and optional `evidence`. If `ready` is omitted, the heartbeat marks the validator ready. This is operational readiness evidence for the local node and does not claim remote public proof until the same validator set is deployed and verified through public endpoints.

`GET /validators/peers` returns persisted validator peer discovery records with `address`, optional `peerId`, optional `host`, optional `p2pAddress`, optional `role`, `expected`, `observed`, `status`, `source`, optional `latestHeight`, optional `lastSeenAt`, optional `updatedAt`, and optional `evidence`. Nodes derive expected peers from `YNX_VALIDATOR_SET` and may receive explicit bootstrap peer records from `YNX_BOOTSTRAP_PEERS` using `address|peerId|host|p2pAddress|role` entries or a JSON array.

`POST /validators/{address}/peers/observe` records observed peer discovery evidence for an existing validator. Request body accepts optional `peerId`, optional `host`, optional `p2pAddress`, optional `status`, optional `latestHeight`, and optional `evidence`. This creates restart-safe local peer-discovery evidence and is required by public-proof checks after remote deployment, but it is not itself proof that remote public validators are live.

`GET /validators/peer-sync` returns persisted peer sync records with `id`, `source`, `target`, `sourceHeight`, `targetHeight`, `lagBlocks`, `status`, optional `evidence`, `createdAt`, and `updatedAt`.

`POST /validators/{address}/peer-sync` records local sync evidence from the source validator in the path to another configured validator. Request body accepts `target`, optional `sourceHeight`, optional `targetHeight`, optional `status`, and optional `evidence`. If `status` is omitted, the node classifies lag within one block as `synced` and larger lag as `lagging`. Source and target must both be configured validators and cannot be the same address. This is restart-safe local sync evidence for deployment readiness and public-proof checks, not a claim that remote node-to-node transport has already been verified.

`ynx-chaind` can also derive peer height-observation records automatically when `YNX_LOCAL_VALIDATOR_ADDRESS`, `YNX_PEER_RPC_URLS`, and optional `YNX_PEER_SYNC_INTERVAL` are set. `YNX_PEER_RPC_URLS` accepts `targetValidator|http://host:port` entries separated by semicolons or a JSON array of `{ "address": "...", "url": "..." }` objects. The node polls each target `/status`, records local source height, target height, lag, and `peer-rpc-poll:<url>/status` evidence, then persists the result through `/validators/peer-sync`. Fresh records, including `fresh_with_lag`, prove current peer reachability and height observation; they do not prove consensus, block replication, or state convergence.

Authoritative testnet replication uses one configured producer and read-only followers. The producer enables `YNX_BLOCK_PRODUCTION_ENABLED`, serves `GET /internal/replication/snapshot` only to callers authenticated by `X-YNX-Replication-Key`, and signs the exact response bytes with HMAC-SHA256 in `X-YNX-Replication-SHA256`. Followers disable block production, configure `YNX_REPLICATION_SOURCE_URL`, verify the response HMAC, validate network identity, contiguous heights, parent hashes, block identities, transaction/block linkage, the Resource sponsor audit digest, and the v2 full-snapshot integrity digest before atomically replacing persistent state. Snapshot v2 covers blocks, pending transactions, accounts, validators, peer observations, Pay, Trust, governance, AI, Resource, contracts, and their nested records. Persistent writes fsync the mode-`0600` temporary file before rename and fsync the parent directory afterward. One marker permits a valid pre-v2 local snapshot to migrate once and rejects later local version downgrades. Replication retains authenticated v1 input only for the bounded follower-first rolling upgrade; v2 is resealed on persistence. The digest detects accidental corruption and unkeyed file changes; it is not a keyed signature, custody control, or independent database audit. Follower mutation requests return HTTP `409`. `GET /status` and `GET /node/identity` expose the non-secret replication lifecycle and exact source/local convergence described above. Verification requires `status=synced`, `catchingUp=false`, `fresh=true`, equal source/local height, and equal source/local hash; a persisted snapshot alone is not enough after restart. This provides deterministic single-chain state convergence and restart recovery; it is not BFT consensus, validator voting, or Byzantine fault tolerance.

`GET /ynx/overview` on `ynx-indexerd` returns the indexed network, chain ID, native currency symbol, latest and earliest-retained source heights, earliest source hash/time, indexed height, block/transaction counts, last sync state, build identity, and truthful service status. It is the public overview contract used by remote verification.

`ynx-chaind --check-config` validates the same node runtime env used by service startup without binding HTTP ports, writing chain state, producing blocks, or starting peer polling. It parses `YNX_VALIDATOR_SET`, `YNX_BOOTSTRAP_PEERS`, `YNX_LOCAL_VALIDATOR_ADDRESS`, `YNX_PEER_RPC_URLS`, and `YNX_PEER_SYNC_INTERVAL`, then fails unsafe multi-validator testnet configs such as missing local identity, local identity outside the validator set, incomplete peer target coverage, self targets, duplicate targets, or targets outside `YNX_VALIDATOR_SET`. Deployment dry-runs validate every generated role-specific env with this command, and real remote deployment runs it after installing `/etc/ynx/ynx-chaind.env` but before restarting `ynx-chaind`.

`ynx-chaind --export-consensus-state <path>` loads the configured persistent state without starting HTTP, block production, replication, or peer polling, then atomically writes a mode-`0600` BFT migration file. The export binds network identity, last committed height/hash, sorted YNXT account balance/stake/nonce/resource/lot state, sorted core validator voting state, resource policy, liquid/staked totals, and a SHA-256 state hash. Operational peer heartbeat/readiness timestamps are deliberately excluded from the consensus hash. `make consensus-migration-check` creates persisted activity, stops the node, exports the file, and independently verifies the commit point, YNXT receiver balance, validator set, and state hash. This is a deterministic migration boundary, not evidence that BFT consensus is already implemented.

`ynx-abci -migration-state <path> [-state <path>]` starts the YNX CometBFT ABCI 2.0 application over `socket` (default `tcp://127.0.0.1:26658`) or `grpc`. The default durable state path is `<migration-state>.abci-state.json`. It validates the migration anchor before listening, binds `InitChain` to `ynx_<numeric-chain-id>-1`, requires the initial height to continue after the migrated commit, and exposes `/migration`, `/state`, `/accounts/{address}`, AI, Pay, Resource, governance, Trust, transparency, `/ide/contracts/{address}`, `/ide/verifier/{address}`, `/ide/call/{address}/{calldata}`, `/evm/receipts/{hash}`, and `/evm/logs` queries. Canonical native transfers and domain-separated `application_action` envelopes use EVM-compatible secp256k1 ownership, chain ID, bounded fees, and one shared account nonce. AI, Pay, Trust, Resource, and bounded IDE actions bind typed canonical payloads. Resource pool lifecycle and sponsorship actions use fee `0`, reserve/consume only committed resource units, and require the actual owner or beneficiary signature; all other application actions retain their defined YNXT fee. IDE deploy/call accepts only the existing pinned artifacts registered for the already-tested bounded interpreter, derives addresses and state from signed input, and persists source/bytecode identity, constructor-seeded storage, SSTORE writes, real LOG records, receipts, audits, and idempotency in AppHash. It does not execute arbitrary source-analyzer artifacts or broaden opcode support. `CheckTx` verifies ownership and state constraints; proposal handlers execute transactions sequentially and reject invalid or duplicate nonce order; `Commit` atomically stores mode-`0600` state before advancing memory. `make bft-ide-contract-check` verifies signed mutation, persistence, tamper rejection, four-application determinism, and Gateway evidence. Bounded IDE also passed a fresh private four-validator candidate, direct-Comet receipt/log comparison, cleanup, and rollback. It is implemented but not persistently or publicly routed.

The BFT Gateway supports `eth_getTransactionByHash` and `eth_getTransactionReceipt` for committed native transfers and signed application actions. It verifies canonical transaction hash, Comet `/tx` proof response (including Comet's structured HTTP-500 not-found envelope), transaction payload hash, block membership, block hash/height, transaction index, `gas_used`, and cumulative gas from `/block_results`; unknown transactions return `null` and inconsistent evidence fails closed. Native and non-contract application actions retain empty logs. Bounded IDE deployment receipts expose the committed contract address; bounded calls expose the committed recipient, storage-write evidence, real LOG records, transaction/block indexes, and a computed Ethereum-compatible logs bloom. `eth_getLogs` validates a retained, at-most-1000-block range and filters AppHash-persisted contract logs by canonical address/topic positions. Native/action and bounded IDE receipt/log behavior have fresh private candidate proof, including direct Comet matching and four-application equality. All fifteen Gateway capabilities are implemented. `publicCutoverReady` defaults to false and can become true only when explicit runtime authorization, all capabilities, a canonical 12-character build commit, a release bound to that commit, and a valid UTC build time are present. That readiness signal is necessary but does not itself execute or prove a public transition.

For an approved authoritative-to-BFT migration, `YNX_BFT_GATEWAY_MIGRATION_HEIGHT` and `YNX_BFT_GATEWAY_MIGRATION_BLOCK_HASH` must be configured together. Health and status expose both values. When serving exactly `migrationHeight + 1`, the Gateway maps an empty Comet `last_block_id` to the approved authoritative block hash so an Indexer copied at the final authoritative height can verify parent continuity. A non-empty different parent is rejected. This compatibility anchor does not make pre-migration blocks available from CometBFT and does not authorize public cutover.

The BFT Gateway accepts EVM JSON-RPC at both `POST /evm` and `POST /`. The root route preserves the existing dedicated EVM-domain and wallet client contract during an ingress transition; it delegates to the same bounded handler and does not add opcode or execution coverage.

`ynx-consensus-lab -ephemeral -local-fixture -output <new-dir>` creates a disposable four-validator local network. For a real migration input, replace `-local-fixture` with `-migration-state <path>`. The generator binds each YNX validator identity to its CometBFT ed25519 public key and consensus address, recalculates the migration hash, emits a common validated genesis and non-secret network manifest, and writes private validator/node keys only as mode-`0600` files. `ynx-consensus-tx -key <raw-32-byte-mode-0600-key> -to <address> -amount <YNXT> -nonce <next>` emits a canonical signed transaction without printing the key. `make consensus-quorum-check` exercises these commands against four real CometBFT and ABCI processes, broadcasts a signed transfer, removes one validator while quorum continues, then restarts and catches it up. These commands prove local behavior only; generated lab keys are forbidden for remote or custody use.

`ynx-consensus-package` validates an exported migration plus a schema-bound, public-key-only four-validator manifest and generates a hash-covered production candidate package. It requires an explicit UTC genesis time and a new output directory. `-verify-package <dir>` rechecks the complete hashed file set and rejects unsafe or private-key filenames. `ynx-consensus-keycheck` runs on each validator host and verifies that mode-restricted CometBFT private validator and node key files derive exactly the public key, consensus address, and node ID approved for that role. The candidate package fixes RPC to `127.0.0.1:27757`, ABCI to `127.0.0.1:27858`, metrics to `127.0.0.1:27660`, P2P to the approved RFC1918 endpoint, and state to `/var/lib/ynx-chain/consensus-candidate`. It does not modify public RPC routes or the authoritative `ynx-chaind` service.

EVM JSON-RPC: `POST /evm` supports `eth_chainId`, `net_version`, `eth_blockNumber`, `eth_getBalance`, `eth_getTransactionCount`, `eth_getTransactionByHash`, `eth_getTransactionReceipt`, `eth_sendRawTransaction`, `eth_sendTransaction`, `eth_estimateGas`, `eth_call`, `eth_getLogs`, `eth_getBlockByNumber`, and `eth_getBlockByHash` in local devnet form. Block lookup now honors the requested canonical height/hash and full-transaction flag; unknown blocks return `null`. Transaction receipts return explicit `null` while a transaction is pending and include canonical block hash, block/transaction index, sender, recipient, gas, and persisted logs after commit. `eth_sendRawTransaction` accepts only `0x`-hex encoded canonical `ynx-native-json-envelope-v1` signed-transfer bytes; `POST /transactions/broadcast` accepts the same canonical JSON bytes directly. Both verify secp256k1 ownership, chain ID `6423`, sender, recipient, amount, fixed fee, and nonce before authoritative state mutation. Exact hash replay is idempotent; changed reuse of an accepted nonce is rejected. Standard Ethereum RLP transaction encoding is not implemented and must not be inferred from the method name. Receipts include persisted EVM-style logs for known transactions, including local contract deployment logs derived from stored contract event metadata and bounded ERC20 transfer logs derived from local contract-call execution. `eth_getLogs` filters persisted logs by `fromBlock`, `toBlock`, `address`, and `topics`. `eth_call` supports source-analyzer pure/view literals for ad hoc IDE snippets and a narrow Hardhat artifact staticcall subset where ABI selectors are generated by `ethers`, recorded in `artifacts/ynx-selector-metadata.json`, required to appear in the solc deployed bytecode, and then executed through a bounded read-only local EVM opcode interpreter for supported static getters such as ERC20 `decimals()`, constructor-seeded `totalSupply()`, and constructor-seeded `balanceOf(address)` mapping reads. Full calldata is accepted for selector matching, and the local interpreter supports Ethereum legacy Keccak `SHA3` for `mapping(address=>uint)` storage reads. `eth_sendTransaction` supports a generic pinned-artifact write-call subset for nonpayable/payable calldata that fits the bounded local EVM state-transition interpreter. Current proof covers ERC20 `transfer(address,uint256)` and `SampleEVMWriteCounter.increment(uint256)`: the interpreter runs solc deployed bytecode, records `SSTORE` writes, captures `executionLogs` from bounded `LOGn` opcodes with Hardhat/ethers event topics, updates canonical local runtime storage slots, returns bounded ABI output when the path is supported, and creates a `contract_call` transaction. ERC20 transfer logs and supported generic bytecode logs such as `CountChanged(address,uint256)` are materialized into local receipts and filterable `eth_getLogs` results after block production. This remains a bounded local execution path, not full EVM state transition support, standard Ethereum transaction compatibility, remote deployment, or remote public proof.

Indexer service: `ynx-indexerd` reads the chain RPC, persists indexed blocks and transactions, and exposes `GET /health`, `GET /metrics`, `POST /sync`, `GET /blocks/latest`, `GET /blocks/{height}`, `GET /txs`, and `GET /txs/{hash}` on the indexer HTTP port. For migration-height sources, an empty database starts at the source's exact `earliestBlockHeight`; resume requires parent-hash continuity with the stored tip and fails with rebuild guidance when the resume point was pruned, the source height regressed, or earliest/latest hashes disagree. Sources without an explicit earliest boundary preserve the authoritative height-`0` behavior.

Explorer service: `ynx-explorerd` reads both RPC and indexer sources and serves the reviewer-facing Explorer web/API surface. It exposes `GET /health`, `GET /metrics`, `GET /api/summary`, `GET /api/stream`, `GET /api/blocks/latest`, `GET /api/blocks/{height}`, `GET /api/txs`, `GET /api/txs/{hash}`, `GET /api/accounts/{address}`, `GET /api/tokens/YNXT`, `GET /api/validators`, `GET /api/resources/{address}`, `GET /api/resource-market/analytics`, `GET /api/fees/{hash}`, and `GET /api/search?q=...`. `GET /api/stream` is an SSE endpoint that emits a `dashboard` event every two seconds containing current summary, latest blocks, latest transactions, validator state, and resource-market analytics; it disables proxy buffering and reports core upstream failures as `upstream-error` events. Validator/resource enrichment failures are reported as snapshot warnings and do not interrupt block/transaction streaming. The Explorer authenticates its server-to-server resource request with `YNX_RESOURCE_GATEWAY_UPSTREAM_KEY`; that key is never returned to the browser. The web UI consumes this stream directly and falls back to ten-second snapshots after a stream failure. Wallet metadata reports native currency `YNXT`.

Faucet service: `ynx-faucetd` is the public faucet backend. It validates addresses, enforces per IP/address rate limits, writes a mode-`0600` JSONL request log, and exposes `GET /health`, `GET /metrics`, `POST /request`, and `POST /faucet`. Explicit `authoritative` mode preserves the privileged legacy RPC funding path. Explicit `bft` mode derives and checks the configured Faucet address from a process-local canonical hex or mode-restricted raw key, serializes account nonce selection, signs chain-`6423` YNXT transfers locally, submits the canonical envelope through `POST /transactions/broadcast`, and verifies committed hash/sender/recipient/amount/fee/nonce/height before reporting success.

Products:

- `POST /faucet`
- `POST /staking/stake`
- `GET /resources/{address}`
- `GET /resource-market/quote`
- `GET /resource-market/analytics`
- `POST /resource-market/delegations`
- `GET /resource-market/delegations/{address}`
- `POST /resource-market/rent`
- `GET /resource-market/income/{address}`
- `POST /resource-market/pools`
- `GET /resource-market/pools`
- `GET /resource-market/pools/{id}`
- `POST /resource-market/pools/{id}/fund`
- `POST /resource-market/pools/{id}/policy`
- `POST /resource-market/pools/{id}/status`
- `POST /resource-market/sponsorships`
- `GET /resource-market/sponsorships`
- `GET /resource-market/sponsorships/{id}`
- `GET /resource-market/sponsor-audit`
- `GET /trust/trace/{address}`
- `POST /trust/labels`
- `POST /trust/evidence`
- `GET /trust/evidence/{id}`
- `GET /trust/evidence/{id}.pdf`
- `POST /governance/requests`
- `GET /governance/requests/{id}`
- `POST /governance/requests/{id}/review`
- `POST /governance/requests/{id}/reject`
- `GET /governance/request-validity-rules`
- `GET /governance/transparency`
- `POST /trust/appeals`
- `GET /trust/appeals/{id}`
- `POST /trust/appeals/{id}/resolve`
- `POST /trust/tracking-reviews`
- `GET /trust/tracking-reviews/{id}`
- `POST /pay/intents`
- `GET /pay/intents/{id}`
- `POST /pay/invoices`
- `GET /pay/invoices/{id}`
- `POST /pay/refunds`
- `POST /pay/webhook-signatures`
- `GET /pay/webhook-signatures/{eventId}`
- `GET /pay/events`
- `GET /pay/events/{id}`
- `GET /ai/stream`
- `POST /ai/permissions`
- `GET /ai/permissions/{id}`
- `POST /ai/actions`
- `GET /ai/actions`
- `GET /ai/actions/{id}`
- `POST /ai/actions/{id}/approve`
- `POST /ai/actions/{id}/reject`
- `GET /ide/compiler`
- `POST /ide/compile`
- `POST /ide/deploy`
- `POST /ide/call`
- `POST /ide/execute`
- `POST /ide/verify`
- `GET /ide/verifier/{address}`
- `GET /contracts/{address}`

`GET /ide/compiler` returns the pinned Solidity compiler configuration used by local IDE artifact and verifier metadata: Solidity `0.8.24`, wasm preference, optimizer settings, config hash, artifact kind, compiler mode, verifier mode, and current limitations.

`POST /ide/compile` returns deterministic local artifact metadata: source hash, bytecode hash, deployed bytecode hash, artifact hash, artifact kind, compiler config hash, compiler execution status, compiler mode, runtime mode, verifier mode, reproducibility status, deployed bytecode comparison status, ABI, parsed events, functions, selector source, `bytecodeSelectorMatched`, local runtime storage seed, local runtime storage slot metadata, and truthful limitations. `POST /ide/deploy` accepts optional `constructorArgs` for local verifier/devnet preflight and preserves them in contract metadata. `POST /ide/verify` and `GET /contracts/{address}` preserve that metadata for developer tooling. `POST /ide/call` and EVM `eth_call` return EVM-style encoded results and expose `executionStatus`, `executionEngine`, `opcodeStepCount`, `artifactKind`, and `bytecodeSelectorMatched` so callers can distinguish source-analyzer results from Hardhat artifact bytecode-interpreter staticcall subset results. `POST /ide/execute` accepts `caller`, `address`, and ABI `calldata` for a bounded local execution path; today it supports a generic pinned-artifact write-call subset for supported nonpayable/payable functions such as ERC20 `transfer(address,uint256)` and `SampleEVMWriteCounter.increment(uint256)`, returns `executionStatus=bounded_local_evm_sstore_state_transition_subset`, exposes `storageWrites` and `executionLogs`, updates canonical local runtime storage via bytecode-subset `SSTORE`, and creates a pending `contract_call` transaction. ERC20 Transfer logs and supported generic bytecode logs become filterable after block production.

For ad hoc IDE snippets, artifact kind remains `source-analyzer-artifact`. If the submitted source exactly matches a repository contract that has a Hardhat artifact built with the pinned Solidity `0.8.24` config, artifact kind upgrades to `pinned-solc-bytecode-artifact` and includes real bytecode/deployedBytecode hashes, ABI functions, `ethers` Keccak selectors, selector-presence checks against the solc deployed bytecode, runtime storage slot metadata, and a local storage seed for simple public primitive state plus constructor assignments such as `totalSupply = initialSupply`, `balanceOf[msg.sender] = initialSupply`, and `SampleEVMWriteCounter.count = initialCount`. `POST /ide/verify` then records `deployedBytecodeComparisonStatus=matched_local_deployed_bytecode_hash`. `POST /ide/execute` and `eth_sendTransaction` can use that metadata for the bounded local write-call path. This is still local devnet evidence, not remote public explorer proof or a complete EVM implementation.

`GET /ide/verifier/{address}` returns explicit local verifier service evidence for a deployed contract: compiler identity, source hash, bytecode hash, deployed bytecode hash, compiler config hash, artifact kind, compiler execution status, verifier mode, reproducibility status, deployed bytecode comparison status, local service status, remote public proof status, and limitations. It deliberately reports `remotePublicProofStatus=not_remote_public_proof` until a real remote verifier or explorer-backed proof is available.

On `ynx-bft-gatewayd`, `POST /ide/deploy` accepts a canonical signed `ide_contract_deploy` envelope and `POST /ide/execute` accepts a canonical signed `ide_contract_call` envelope; the private key never enters the Gateway request as a field. `POST /ide/call` remains a read-only JSON request with `address` and `calldata`. `GET /ide/contracts/{address}` returns committed contract state and `GET /ide/verifier/{address}` returns committed pinned source/deployed-bytecode identity with `remotePublicProofStatus=not_claimed`. Mutation success is returned only after Comet commit, block membership, and ABCI receipt/contract evidence agree. These handlers passed private-candidate proof and rollback but are not currently deployed or publicly routed.

Verification:

```bash
make smoke-test
```

The smoke test exercises RPC health, EVM chainId, block growth, faucet funding, transfer lookup, EVM receipt/log filtering, contract artifact metadata, IDE and EVM pure/view call behavior, contract-address/topic log filtering, AI streaming, Trust label/evidence/PDF export, Pay intent/invoice/refund/webhook signature, resource quote/delegation/rental/income/analytics, IDE deploy, contract verification, monitoring, indexer sync, Explorer API summary, public faucet daemon funding, and package lists. The contract tooling gate additionally proves Hardhat selector metadata such as ERC20 `decimals()` selector `0x313ce567`, `totalSupply()` selector `0x18160ddd`, `balanceOf(address)` selector `0x70a08231`, `transfer(address,uint256)` selector `0xa9059cbb`, `Transfer(address,address,uint256)` event topic, `SampleEVMWriteCounter.count()` selector `0x06661abd`, `increment(uint256)` selector `0x7cf5dab0`, and `CountChanged(address,uint256)` event topic are present. The API tests prove constructor-seeded `totalSupply()`, mapping-backed `balanceOf(address)`, bounded bytecode-subset ERC20 `transfer(address,uint256)` SSTORE writes, generic pinned-artifact write-call subset execution for `SampleEVMWriteCounter.increment(uint256)`, canonical local storage updates, bounded return decoding, `executionLogs`, filterable Transfer logs, and filterable generic CountChanged logs. It returns non-zero on failure.

Governance and Trust request safety:

- `ynx-trustd` is the independent public Trust and Chain Law API on port `6431`. Protected `/trust/*` and `/governance/*` routes require `X-YNX-Trust-Key` or `Authorization: Bearer <YNX_TRUST_API_KEY>` and return a unique `X-Request-ID`.
- The gateway enforces a 1 MiB request-body limit, a 2 MiB evidence/export response limit, per API-key/IP rate limits, and fail-closed pre-forward audit. Deployed `ynx-chaind` rejects direct Trust/Governance bypass without `YNX_TRUST_GATEWAY_UPSTREAM_KEY`.
- Gateway JSONL audit stores request metadata, body hashes, status, outcome, and audit hashes. It does not store request bodies, API keys, upstream keys, private keys, seed phrases, appeal evidence, or reviewer notes.
- `POST /governance/requests` stores a lawful-request intake record and classifies it as `VALID_UNDER_YNX_CHAIN_LAW`, `INSUFFICIENT_EVIDENCE`, `OUT_OF_SCOPE`, `OVERBROAD`, `ILLEGAL_OR_ABUSIVE`, `REQUIRES_GOVERNANCE_REVIEW`, `REQUIRES_USER_NOTICE`, or `REJECTED`.
- `GET /governance/requests/{id}` retrieves the persisted request, including classification, status, rule IDs, timestamps, and transparency entry ID.
- `POST /governance/requests/{id}/review` records explicit governance review status and `reviewedAt`; `POST /governance/requests/{id}/reject` records reviewer rejection reason, `REJECTED` classification, `rejectedAt`, and a transparency entry.
- `GET /governance/request-validity-rules` returns the named Chain Law / Request Validity rules used by governance and tracking review classification. Request and tracking-review responses include `ruleIds` so a reviewer can inspect why a classification was reached.
- Native `YNXT` requests cannot directly transfer, freeze, seize, blacklist, or bypass user signatures. Illegal, overbroad, evidence-free, off-scope asset-boundary, hidden-record, fake-risk, unsupported-Trust-conclusion, and AI-auto-punishment requests are rejected and recorded in the transparency report.
- Evidence-backed notification-only requests are classified as `REQUIRES_USER_NOTICE` with `notice_required` status; they create transparency records but do not perform asset action.
- `POST /trust/labels` records advisory-only address or transaction risk metadata including `labelId`, `subject`, `subjectType`, `address`, `labelType`, `severity`, `riskWeightBps`, `confidenceBps`, `source`, `evidenceHash`, `expiresAt`, `reviewRequired`, `appealAvailable`, `disputeStatus`, `legalStatusUnderYnxChainLaw`, optional rejected-request reference, and `assetEffect`. `subjectType` defaults to `address`; `transaction` requires an existing 32-byte transaction hash and leaves `address` empty. `assetEffect` must remain `none_advisory_only`; labels do not freeze, seize, confiscate, transfer, or classify users as criminals.
- `POST /trust/evidence` and `GET /trust/evidence/{id}` return `riskSummary` with effective advisory risk weight, active/expired/low-confidence/correction label counts, non-conclusive label IDs, active evidence hashes, reviewer notes, appeal path, and `assetEffect`. Expired labels and labels below 5000 confidence bps are kept for audit context but excluded from active risk scoring. Appeal correction labels created by false-positive decisions must remain visible through `riskSummary.correctionLabelCount` and reviewer notes.
- `POST /trust/appeals` opens an appeal record for false-positive correction or dispute review and also creates a transparency entry. Appeals must reference an existing governance/tracking-review `requestId` or an existing Trust `labelId`; missing or mismatched references are rejected so transparency entries cannot be created from orphaned appeals.
- `GET /trust/appeals/{id}` retrieves the persisted appeal, including request linkage, appellant, evidence, status, reviewer/decision fields when resolved, and transparency entry ID.
- `POST /trust/appeals/{id}/resolve` records reviewer, decision, updated status, and resolution reason. Decisions include `UNDER_REVIEW`, `NEEDS_MORE_EVIDENCE`, `ACCEPTED`, `REJECTED`, `LABEL_REMOVED`, and `LABEL_REDUCED`; accepted/removal/reduction decisions add corrective Trust labels rather than freezing or moving funds. Remote public proof must show that the correction is reflected in Trust evidence summary after resolution.
- `POST /trust/tracking-reviews` records purpose-limited tracking review metadata: requester, subject, purpose, query type, scope, evidence, institutional/sensitive flags, minimum-necessary status, confidence, expiry, classification, and appeal path. Overbroad, evidence-free, sensitive-inference, low-confidence punitive, or audit-bypass tracking requests are rejected or routed to review.
- `GET /governance/transparency` returns the locally persisted transparency report entries and counts. Remote public proof must use the public endpoint, not localhost.

Resource Market API safety:

- `ynx-resourced` is the independent public Resource Market API on port `6432`. Protected `/resource-market/*` routes require `X-YNX-Resource-Key` or `Authorization: Bearer <YNX_RESOURCE_API_KEY>` and return a unique `X-Request-ID`.
- The gateway enforces a 1 MiB request-body limit, a 2 MiB response limit, per API-key/IP rate limits, and fail-closed pre-forward audit. Deployed `ynx-chaind` rejects direct Resource Market bypass without `YNX_RESOURCE_GATEWAY_UPSTREAM_KEY`.
- Gateway JSONL audit stores request metadata, body hashes, status, outcome, and audit hashes, but not request bodies or credentials. Canonical policy, quote, delegation, rental, provider/protocol income, merchant/dApp pool, sponsorship, idempotency, and append-only sponsor-audit state remains in the persistent chain runtime.
- `POST /resource-market/pools` creates a `merchant` or `dapp` pool. `fund`, `policy`, and `status` mutations require a versioned domain-separated secp256k1 authorization from the canonical pool owner, bind the chain ID, action, request hash, and next account nonce, and support exact idempotent replay. Status transitions accept `active`, `paused`, or irreversible `revoked`; revocation releases only unused reserved resources.
- A pool policy binds public or allowlisted beneficiaries, operation scopes, resource types, per-action limits, funded cumulative allowance, expiry, and policy hash. Funding reserves the owner's existing Bandwidth, Compute, AI Credits, or Trust Credits; it does not transfer YNXT or user assets.
- `POST /resource-market/sponsorships` requires the beneficiary's own signature and next nonce. It deterministically selects the lexicographically first eligible pool when `poolId` is omitted, atomically consumes allowance, and records payer, sponsor, pool, resource type/source, amount, policy hash, action reference, transaction hash, and idempotency key. It never changes the sender, signs for a user, moves token balances, or creates an admin debit.
- Pool/sponsorship state, action-reference uniqueness, exact replay records, and the hash-chained audit log survive restart and are covered by a sponsor-state integrity digest. Changed replay, stale nonce/policy, wrong owner, expired/paused/revoked pool, disallowed beneficiary/scope/type, per-action overflow, exhausted allowance, duplicate action reference, and concurrent overspend fail closed.
- The authenticated public Resource Market surface includes policy, quote, delegation, rental, income, analytics, pool lifecycle/read APIs, sponsorship create/read APIs, and sponsor audit. Explorer fee detail reads sponsorship fields from indexed transactions rather than inserting a narrative sponsor.
- `YNX_RESOURCE_GATEWAY_UPSTREAM_MODE=authoritative` preserves the current rollback-compatible proxy. Candidate `bft` mode requires chain ID `6423`, a canonical signer address, and exactly one process-local raw private-key source, preferably a mode-`0600` file, for the legacy delegation/rental service workflow. The daemon injects that signer only as delegation provider or rental address, reads the committed policy and quote, serializes nonce allocation, signs those canonical actions locally, and verifies committed evidence. Pool lifecycle and sponsorship routes are different: they accept and relay an already-signed owner/beneficiary envelope, never use the service key, and verify the returned transaction/object binding. Exact replay returns the original committed object without another nonce or fee; changed-input reuse returns `409`.
- BFT delegation converts signer liquid YNXT to beneficiary staked YNXT. BFT rental transfers the policy quote from renter to provider and protocol treasury, persists the quote commitment, rental, income, event, and idempotency records in AppHash, and reduces only already-used resource counters. The fixed action fee is `1 YNXT`; Resource actions do not consume AI, Pay, or Trust units. Total liquid plus staked YNXT must remain equal to the migration anchor.
- These BFT Resource paths passed a fresh private four-validator candidate, complete four-application Resource/account/AppHash equality, four-signer evidence, capability promotion, cleanup, and rollback. They are still not persistently deployed or enabled on public routing; this is candidate compatibility evidence, not public BFT proof.
- Sponsor pools and sponsored resource actions now have local deterministic BFT support. Canonical owner-signed create/fund/policy/status actions and beneficiary-signed consumption actions commit pool state, allowance/consumption, exact replay snapshots, action-reference uniqueness, and hash-chained audit records into AppHash. Four independent applications produced identical state/AppHash; restart, tamper, pause/expiry/revoke, allowance, nonce, route binding, BFT Gateway commit evidence, Resource Gateway client-signed relay, and Explorer payer/sponsor/source mapping tests pass. `ynx-resourced` forwards these already-signed bodies and never uses its service signer for pool-owner or beneficiary actions. This code is not on the current remote release and has no public BFT transaction or independent public Explorer proof.

Bridge coordinator API safety:

- `ynx-bridged` is a standalone local coordinator on port `6433`. `GET /health` and `GET /metrics` are public process surfaces; every `/bridge/*` route requires `X-YNX-Bridge-Key` or `Authorization: Bearer <YNX_BRIDGE_API_KEY>`.
- `POST /bridge/transfers` creates a persistent transfer intent. The request binds an idempotency key, source chain/transaction/event index, source and destination assets, amount, sender, and recipient. `GET /bridge/transfers` lists intents and `GET /bridge/transfers/{id}` returns one intent.
- Each configured route fixes the source/destination chain and asset pair, maximum unsigned 64-bit amount, minimum confirmations, and `canonical-to-represented` or `represented-to-canonical` asset boundary. Configuration rejects same-chain routes, zero finality, duplicate routes, invalid amounts, and any route with external submission enabled.
- `POST /bridge/transfers/{id}/attestations` accepts one Ed25519 signature per allowlisted relayer over the canonical transfer/finality payload. It rejects unknown relayers, invalid signatures, insufficient confirmations, changed duplicate votes, and source block disagreement. At least two distinct relayers and a threshold of at least two are mandatory.
- `POST /bridge/transfers/{id}/finalize` changes only local coordinator state after finality and threshold quorum. Exact replay returns the original result; changed idempotency reuse and double-finalize attempts fail closed. It does not mint, burn, freeze, seize, submit an external transaction, or move native YNXT.
- Persistent state is atomically replaced in a mode-`0700` directory as a mode-`0600` JSON file. It includes globally unique source-event indexes, transfer and idempotency records, a hash-chained audit log, and an overall integrity digest. Startup rejects malformed or tampered state; this is local tamper detection, not a substitute for production custody or an external database audit.
- `GET /bridge/audit` returns the append-only audit chain. Health and metrics always report `externalSubmissionEnabled=false`, `liveBridge=false`, and `truthfulStatus=local-coordinator-only-no-external-submission` in this implementation.
- The release bundle contains the binary, dedicated `0600` env, systemd unit, config check, backup/state paths, and restart health check. Remote installation is gated by `YNX_BRIDGE_DEPLOY_ENABLED=true`; it remains disabled in the real deployment environment until production relayer custody, route review, and a separately approved deployment exist.
- `make bridge-api-check` proves local race safety, restart persistence, exact replay/conflict behavior, strict JSON/auth boundaries, state modes, and truthful health. No remote public Bridge endpoint, external-chain transaction, live mint/burn, third-party bridge integration, or stablecoin issuer support is claimed.

Stablecoin Issuer Control API:

- `ynx-stablecoind` is a standalone operator control plane on loopback port `6434`. `GET /health` and `GET /metrics` are process surfaces; every `/stablecoin/*` route requires `X-YNX-Stablecoin-Key` or `Authorization: Bearer <YNX_STABLECOIN_API_KEY>`.
- `POST /stablecoin/issuers`, `GET /stablecoin/issuers`, and `GET /stablecoin/issuers/{id}` create and inspect issuer candidates. `POST /stablecoin/issuers/{id}/review` records an explicit approve/reject decision, and `POST /stablecoin/issuers/{id}/revoke` removes an approved issuer's authority for new assets and intents. Decisions require reviewer identity, governance request reference, SHA-256 evidence, reason, and exact idempotency.
- `POST /stablecoin/assets`, `GET /stablecoin/assets`, and `GET /stablecoin/assets/{id}` persist issuer-bound asset candidates with canonical/represented classification, origin chain and contract reference, decimals, supply ceiling, reported supply, mint/burn policy, legal-review status, and evidence hashes. `POST /stablecoin/assets/{id}/review` records approval/rejection; `POST /stablecoin/assets/{id}/revoke` stops new intents.
- `POST /stablecoin/assets/{id}/intents` records a `mint` or `burn` intent for an approved issuer and asset. It reserves bounded unsigned 64-bit supply capacity and rejects changed idempotency reuse, wrong issuers, revoked authority, missing evidence, mint amounts above remaining ceiling, and burn amounts above reported supply. `GET /stablecoin/intents` and `GET /stablecoin/intents/{id}` expose the immutable non-executing records.
- Native `YNXT`, wrapped/native YNXT identifiers, gas/resource balances, validator stake/bond, and protocol treasury state are outside issuer authority. There is no freeze, seize, blacklist, native transfer, external submission, or token execution route. Unknown JSON fields are rejected, and health/metrics always report issuer support, external execution, and native YNXT issuer actions as false.
- `GET /stablecoin/audit` returns the hash-chained audit sequence. `GET /stablecoin/transparency` reports application, approval, rejection, revocation, and mint/burn-intent counts while executed and native-protocol action counts remain zero.
- State is atomically replaced in a mode-`0700` directory as a mode-`0600` JSON file. Startup checks the state digest, audit chain, registries, governance lifecycle, idempotency references, supply reservations, execution-disabled flags, and native-asset boundary. The digest detects corruption; it is not a keyed signature or a substitute for independent custody/database controls.
- Governance request IDs and evidence hashes are bound references, not remote validation that the referenced governance or legal review actually occurred. Every issuer keeps `supportStatus=candidate_not_supported`; service health keeps `truthfulStatus=local-control-plane-only-no-issuer-support-no-execution`.
- The release package carries the binary, dedicated env, systemd unit, backup/state paths, config check, and optional health check. Real deployment remains default-disabled behind `YNX_STABLECOIN_DEPLOY_ENABLED=false`, and no ingress is configured.
- `make stablecoin-issuer-check` proves issuer/asset approval and revocation, native/protocol asset rejection, supply bounds, exact replay/conflict, concurrency, restart persistence, tamper rejection, strict auth/JSON, transparency, truthful metrics, and file modes. It does not prove issuer support, mint/burn execution, remote deployment, public availability, or a third-party partnership.

YNX Chat API:

- `ynx-chatd` is a standalone encrypted-envelope service on loopback port `6435`. `GET /health` and `GET /metrics` expose process state. Every `/chat/*` route requires `X-YNX-Chat-Key`; device-authenticated routes additionally require `X-YNX-Device-ID`, `X-YNX-Timestamp`, and `X-YNX-Device-Signature` over the method, exact request URI, timestamp, and body hash.
- `POST /chat/devices` registers an Ed25519 signing key and X25519 encryption key against a normalized `ynx1...` account after private-key proof. EVM `0x...` identities are rejected at this first-party boundary. `POST /chat/devices/{id}/revoke` prevents further signed requests from the device.
- `POST /chat/devices/{id}/rotate` atomically revokes one same-account device and registers a new device. The request requires an explicit signature from the authenticated active authorizing device and a separate private-key proof from the new device, plus exact idempotency. `GET /chat/device-rotations` returns only the authenticated account's hash-only rotation records. This supports bounded recovery while another account device is active; it is not social recovery or hardware-custody proof.
- `POST /chat/conversations` creates one direct conversation between two distinct `ynx1...` members, each with an active device. `GET /chat/conversations` returns only the authenticated member's conversations in reverse update order. `GET /chat/conversations/{id}` and `GET /chat/conversations/{id}/messages` enforce membership.
- `GET /chat/accounts/{account}/devices` returns signing/encryption public keys and active/revoked status only when the authenticated account is the same account or already shares a conversation with the exact member. The route is not a public account directory. Revoked public keys remain visible to shared members so historical ciphertext can still be authenticated, while new sends must select active devices only.
- `POST /chat/conversations/{id}/messages` v2 requires exactly one `x25519-hkdf-sha256-xchacha20poly1305` envelope for every active device of both conversation members. One logical message uses one ephemeral X25519 public key and a unique nonce per device; every envelope binds the conversation, message, sender device, recipient account/device, algorithm, ephemeral key, nonce, and ciphertext. The sender device signs the canonical complete envelope set. Missing, duplicate, extra, revoked, mixed-key, changed-replay, bad-hash, and bad-signature inputs fail closed. The service never accepts or persists a plaintext field. `POST /chat/conversations/{id}/messages/{messageId}/delivered` and `/read` persist v2 acknowledgements per device; legacy v1 records remain readable with account-level acknowledgements.
- Device registration, conversation creation, and message IDs are exact-replay safe; changed reuse fails with `409`. Requests are size bounded, rate limited per device/IP, timestamp bounded, unknown-field rejecting, and protected by the shared mutation-freeze wrapper.
- State is atomically replaced as mode `0600` JSON with an integrity digest and redacted hash-chained audit events. This provides restart recovery and corruption detection, not multi-region durability or an independent security audit.
- The release bundle carries the binary, dedicated env, systemd unit, backup/state paths, config check, and optional health verification. Exact Chat and App Gateway release `ynx-chain-c6a0e908e184` is installed remotely on loopback with preserved configuration/state and scoped rollback. The Gateway supplies the separate native chain-account proof before a browser or bound native client can reach registration or any Chat route. Public TLS rejects unauthenticated rotation/list calls. An operator-controlled signed smoke completed empty conversation, self-device-directory, and rotation-record reads followed by Chat/Square device and session cleanup without creating a post, conversation, message, or rotation; this is not installed-App or independent public proof.
- `make native-wallet-check`, `make chat-api-check`, `make app-account-ownership-check`, mobile protocol tests, and a shared Go/TypeScript envelope vector prove native-only identity formatting, device-key proof, E2EE interoperability, account/device session binding, signed HTTP routes, member-directory privacy, replay/conflict handling, revocation, acknowledgements, rate limiting, restart/tamper behavior, truthful health, and file modes. The native App includes a locally verified direct-message window, but no installed or public message write has been executed. Multi-device fan-out/recovery, groups, attachments, Pay/Trust integration, independent audit, and WeChat-equivalent completeness remain unproven.

YNX Square API:

- `ynx-squared` is a standalone signed social-record service on loopback port `6436`. Process health and metrics are public locally; `/square/*` requires `X-YNX-Square-Key`. Mutations and private report reads additionally require an active `ynx1...` device, bounded timestamp, and Ed25519 signature over the exact method, request URI, timestamp, and body hash.
- `POST /square/devices` registers an Ed25519 public key after private-key proof. `POST /square/devices/{id}/revoke` disables the device. The first-party boundary rejects `0x...` identity input.
- `POST /square/posts` creates a bounded signed post with optional normalized tags. `GET /square/posts/{id}` returns one active post, and `GET /square/feed?limit=&cursor=` returns deterministic reverse-time cursor pagination from persisted records.
- `POST /square/posts/{id}/comments` and `GET /square/posts/{id}/comments` create and list bounded comments. `POST /square/posts/{id}/reactions` sets or removes one `like`, `insight`, or `support` reaction per account while maintaining persisted post counts.
- `POST /square/follows` sets or removes a signed follow relationship. `GET /square/profiles/{ynx1Account}/following` returns the normalized active following list.
- `POST /square/profiles` creates or updates the authenticated account's unique Social `handle`, bounded display name, and bio. Handles normalize an optional leading `@` to lowercase `[a-z][a-z0-9_]{2,23}`, reject reserved names, and conflict if another account already owns the value. `GET /square/handles/{handle}` publicly resolves an exact handle to its profile and underlying signing account; `GET /square/profiles/{ynx1Account}` returns the profile plus persisted follower, following, and active-post counts. The handle is the user-facing discovery identity while the normalized `ynx1...` account remains the cryptographic principal.
- Comments, newly activated reactions, and newly activated follows atomically create member-scoped notifications for the affected account. `GET /square/notifications?limit=&cursor=` returns only the authenticated device account's reverse-time notification feed and unread count. `POST /square/notifications/{id}/read` records an exact-idempotent acknowledgement; another account cannot list or acknowledge the record.
- `POST /square/reports` opens a `pending_review` report against an existing post/comment or normalized account with bounded category/detail and optional SHA-256 evidence references. `GET /square/reports/{id}` is restricted to the reporting account. `appealRoute=/trust/appeals` is a route reference only; automatic Trust appeal creation and moderation decisions are not implemented yet.
- The native client maps these exact Gateway routes to strict comment, reaction, follow, report, profile, handle-resolution, notification-list, and notification-read methods. Chat creation accepts `@handle` or `ynxsocial://profile/{handle}`, resolves it through Square, and supplies the resulting account only to the encrypted protocol. Conversation rows show display name and handle instead of using a chain address as a friend identifier. The separate YNX Social package exposes Feed, Messages, and Alerts only; the separate YNX Wallet package exposes Wallet only. These product bundles and route policies are locally verified but are not remotely deployed or store released at this checkpoint.
- Every mutation is exact-idempotent; changed key reuse fails with `409`. Requests are body/rate/timestamp bounded, unknown-field rejecting, and protected by the shared mutation-freeze wrapper. State is mode-`0600` atomic JSON with an integrity digest and hash-chained audit.
- The release bundle includes a default-disabled `YNX_SQUARE_DEPLOY_ENABLED` gate, binary, secret env, systemd unit, state/backup paths, config check, and optional local health verification. Exact Square and App Gateway release `ynx-chain-09598d2cbcd7` is installed remotely on loopback behind public TLS ingress. Public feed/profile reads are sessionless through the App Gateway; mutations, private notifications, and private-report routes require the ownership-bound session plus the upstream device signature.
- `make square-api-check` proves signed profile/post/comment/reaction/follow/notification/report lifecycle, feed pagination, private notification access and read acknowledgement, exact replay/conflict, access/rate bounds, current and legacy-v1 state restart, tamper rejection, health/metrics, and file modes. Direct service device proof alone does not prove control of the claimed on-chain account; the App Gateway supplies that separate ownership proof. Exact Square/Gateway release `09598d2cbcd7` is remotely active and operator-controlled public TLS checks prove exact-build health, a live public profile read, and unauthenticated notification denial. The exact test-only Android Release passes an emulator-installed Feed/Messages/Alerts/Pay UI gate and a separate non-destructive Pixel 9 Feed/Alerts read/UI check, but no installed signed Social mutation, owner-approved public record, production package, or independent proof is inferred.

First-party App Gateway:

- `ynx-app-gatewayd` listens on loopback port `6437` and is the supported first-party browser/native ingress. Six Square read families are public without a session: feed, post, comments, profile, handle resolution, and following. Profile updates, notification list/read acknowledgement, all other Square mutations, device controls, and private reports require the ownership session. It rewrites accepted `/app/{service}/*` paths to loopback `/{service}/*` paths before forwarding.
- `POST /app/session/challenges` accepts an exact `ynx1...` account, bounded device ID, and raw-base64 Ed25519 device signing public key. It returns a five-minute canonical `YNX_APP_ACCOUNT_OWNERSHIP_V1` sign document bound to chain ID `6423`, a cryptographically random nonce, account, device, client binding, issue time, and expiry. Browsers bind to one configured exact HTTPS origin. The React Native client sends `X-YNX-Client: ynx-mobile-v1` without a browser origin and binds the signature to `ynx-mobile://com.ynxweb4.mobile`.
- `POST /app/session/challenges/{id}/verify` requires both a compressed secp256k1 public key plus canonical low-S DER signature over SHA-256 of the sign document and an Ed25519 signature over the same bytes. The secp256k1 key must derive the claimed account. Challenges are one-time and fail on replay, expiry, wrong account/key/device/origin, malformed DER, high-S, or changed sign bytes.
- Successful verification returns a cryptographically random `X-YNX-App-Session` token bound to the exact account, device, browser/native client binding, and a 30-minute expiry. Only its SHA-256 hash is persisted. `POST /app/session/revoke` revokes the current token. Restart preserves active sessions; expired and revoked records are retained only for a bounded period and total challenge/session/audit records are capped.
- A valid account-bound session is required for both Chat/Square device registration, every Chat route, every Square mutation, device revocation, and private report reads. Registration JSON must match the session account, device ID, and device signing public key. The upstream services still independently verify the device registration proof and every timestamped Ed25519 request signature.
- Device HTTP signatures use the canonical service request URI after gateway rewriting: clients call `/app/square/...` or `/app/chat/...` but sign `/square/...` or `/chat/...`, including the exact query string. The gateway never has a device private key and cannot rewrite or replace the signature.
- Metrics paths, arbitrary upstream paths, path escapes, unsupported methods, unknown services, missing sessions, mismatched registrations, wrong origins, expired sessions, and revoked sessions fail closed. Public Square reads remain sessionless and read-only.
- `X-YNX-Chat-Key`, `X-YNX-Square-Key`, and `X-YNX-Pay-Key` are loaded from the gateway's mode-`0600` server environment and injected only on allowlisted loopback requests. Incoming service-key headers, cookies, and authorization headers are discarded. Browsers and native clients receive no service credential.
- Protected browser access requires an exact configured HTTPS `Origin`; no wildcard, HTTP origin, origin path, cookie, or bearer authorization fallback exists. Native access accepts only one exact product client without a browser origin: internal `ynx-mobile-v1` binds to `ynx-mobile://com.ynxweb4.mobile`, Social `ynx-social-v1` binds to `ynx-social://com.ynxweb4.social`, and Wallet `ynx-wallet-v1` binds to `ynx-wallet://com.ynxweb4.wallet`. Social is limited to Chat/Square protected routes; Wallet receives no protected Chat/Square/Pay route through this Gateway. Mixed browser/native bindings are rejected, and account/device signatures remain mandatory.
- State uses atomic mode-`0600` JSON under a mode-`0700` directory with a corruption-detection digest and bounded hash-chained audit events. Raw session tokens, account private keys, device private keys, seed phrases, service keys, request bodies, and signatures are not persisted in this state. This is not a hardware-wallet recovery design, keyed tamper-proof ledger, multi-region session store, or independent security audit.
- Request and response sizes, upstream duration, and request rate are bounded. Responses copy only content type and status and always use `Cache-Control: no-store`; upstream internal headers are not exposed.
- `GET /health` is loopback operations health. `GET /app/health` exposes the same bounded health through the application ingress and reports Chat/Square/Pay upstream status, build identity, ownership/session boundary, active non-expired session count, and whether the gateway was configured as remotely deployed.
- Release installation is default-disabled behind `YNX_APP_GATEWAY_DEPLOY_ENABLED=false`. Nginx/Caddy route only `/app/*` to this daemon; direct Chat, Square, and merchant Pay services remain credential-protected loopback upstreams for App traffic.
- `make app-gateway-check` and `make app-account-ownership-check` prove low-S account-key derivation, browser and native-client separation, device dual-signature binding, wrong-key/origin/device denial, replay/expiry/revocation, restart persistence, state tamper rejection, token-hash-only storage, mode checks, real Chat/Square device registration, a signed Square mutation, signed Chat list/device route allowlisting, public read behavior, exact origin/CORS/bounds, server-side credential isolation, and direct-service denial. Scoped release `ynx-chain-376c95793d66` is remotely active with `nativeBoundary=ynx-mobile-v1`; its binary, env, unit, and state were backed up before restart. Operator-controlled public-TLS and native-bound smokes completed disposable ownership, device registration, a signed empty Chat-list read, signed device revocation, and session revocation without a post, conversation, or message. This is protocol evidence plus separate Android emulator rendering, not installed authenticated messaging, independent proof, or an installed iOS/real-device app. No production native package, store release, macOS/Windows package, or third-party audit is claimed.

Pay merchant safety:

- `ynx-payd` is the independent public merchant API on port `6430`. Its protected `/pay/*` routes require `X-YNX-Pay-Key` or `Authorization: Bearer <YNX_PAY_API_KEY>` and return a unique `X-Request-ID`.
- `POST /pay/intents`, `POST /pay/invoices`, `POST /pay/refunds`, and `POST /pay/webhook-signatures` require `idempotencyKey` at the public gateway. Reusing the same key in the same merchant/object scope returns the original object instead of creating a conflicting duplicate. The authenticated merchant identity is injected from `YNX_PAY_MERCHANT_ID`; a conflicting client merchant is rejected.
- `POST /pay/webhook-signatures` stores replay-auditable metadata: `eventId`, `eventType`, `payloadHash`, `algorithm`, `signedAt`, `idempotencyKey`, and `replaySafe`. The HMAC key is loaded from `YNX_PAY_WEBHOOK_SIGNING_KEY`; clients cannot supply it, and it is never returned or stored in response or audit objects.
- `GET /pay/webhook-signatures/{eventId}` returns a stored signature for audit/replay verification.
- `GET /pay/events?intentId=...` and `GET /pay/events/{id}` return persisted payment audit events with `auditHash`, object id, amount, currency, merchant, and idempotency key metadata.
- `ynx-payd` enforces a 1 MiB request-body limit, per API-key/IP rate limits, fail-closed pre-forward access audit, and a dedicated upstream secret. Deployed `ynx-chaind` rejects direct `/pay/*` bypass attempts without `YNX_PAY_GATEWAY_UPSTREAM_KEY`. JSONL access audit contains only request metadata and body hashes, never bodies or credentials.
- `YNX_PAY_GATEWAY_UPSTREAM_MODE=authoritative` preserves the current rollback-compatible chain proxy. Candidate `bft` mode requires chain ID, a configured signer address, and exactly one process-local raw key source. It checks committed idempotency before nonce allocation, signs canonical Pay actions locally, sends only signed envelopes to the loopback BFT Gateway, verifies committed transaction/object evidence, and computes webhook HMAC locally with a separate key. Identical replay returns the committed object without another nonce or fee; changed input fails with `409`. This mode is locally verified only and is not enabled on public routing.
- Checkout-capable `POST /pay/intents` accepts `payoutAddress`; valid EVM-compatible input is normalized and persisted as canonical `ynx1...`, and invoices inherit that immutable payout binding. Legacy intents without a payout remain readable but cannot be settled.
- `POST /pay/invoices/{id}/settle` requires `payer`, a canonical committed native `transactionHash`, and `idempotencyKey`. Authoritative state verifies transfer type, payer, payout recipient, exact invoice amount, fixed `1 YNXT` fee, block commitment, invoice creation time, expiry, and global transaction uniqueness before atomically marking the invoice and intent paid. Exact replay returns the original settlement; changed input, pending/mismatched/reused transactions, paid/expired invoices, and unbound legacy invoices fail closed.
- `GET /pay/invoices/{id}/settlement` returns the persisted payer, payout, transaction hash, block number, amount, status, and audit hash. The paid transition also appends a persisted `invoice.paid` event.
- App clients use `GET /app/pay/invoices/{id}`, `GET /app/pay/invoices/{id}/settlement`, and protected `POST /app/pay/invoices/{id}/settle`. The protected route requires the existing ownership session, rejects any client-supplied `payer`, and injects the session's verified `ynx1...` account. Candidate BFT mode does not yet implement settlement and returns an explicit error instead of falling back or fabricating success.
- `make pay-api-check` locally proves payout binding, faucet funding, a real native transfer, block commitment, settlement, receipt lookup, audit, and idempotency. These consumer routes and the App Gateway Pay upstream are not yet remotely deployed; no public consumer payment or value transfer is claimed.
- Remote public proof must show Pay idempotency replay for intents and invoices, replay-safe webhook metadata, webhook lookup/replay behavior, and audit-hashed Pay events on public endpoints.

AI Gateway permission and audit:

- `ynx-ai-gatewayd` is the independent public AI service on port `6429`. `GET /health` exposes chain identity, provider configuration state, model name, rate-limit policy, build identity, and `truthfulStatus=chain-context-and-provider-backed-ai-gateway`; `GET /metrics` exposes request, success, denial, error, and active-request counters.
- Protected routes require `X-YNX-AI-Key` or `Authorization: Bearer <YNX_AI_GATEWAY_API_KEY>`. The provider key and access key are loaded only from the gateway's `0600` env file. In rollback-compatible `authoritative` mode, `YNX_AI_GATEWAY_UPSTREAM_KEY` authenticates gateway-to-chain proxy calls. In `bft` mode, exactly one process-local signer key source must match `YNX_AI_GATEWAY_SIGNER_ADDRESS`; the gateway serializes account nonce selection, signs canonical application actions locally, and sends only signed envelopes to the loopback BFT Gateway.
- `GET /ai/stream?session=...&q=...` calls the configured OpenAI-compatible provider with current YNX chain status context and streams isolated SSE `metadata`, `token`, and `done` events carrying a unique request ID. It does not execute value-moving or Trust-affecting actions.
- Gateway JSONL audit entries contain request/session/path/status/outcome, prompt hash, and audit hash. Raw prompts, access keys, provider keys, private keys, and seed phrases are not written.
- `POST /ai/permissions` creates an explicit scoped permission grant with `sessionId`, `requester`, `scope`, `purpose`, expiry, status, and `auditHash`; `GET /ai/permissions?sessionId=...` lists grants through the same authenticated service boundary.
- `POST /ai/actions` stores an AI action proposal with `sessionId`, `requester`, `scope`, `actionType`, `description`, sensitivity metadata, executable status, reasons, `auditHash`, and transparency entry.
- Sensitive AI actions include value movement, Trust label/risk-state changes, and sensitive-data or evidence export. They are persisted as `pending_approval` and `executable=false`.
- `POST /ai/actions/{id}/approve` requires a matching active permission for the same session, requester, and scope before a sensitive action can become `approved` and `executable=true`.
- `POST /ai/actions/{id}/reject`, `GET /ai/actions?sessionId=...`, and `GET /ai/actions/{id}` expose auditable review state without bypassing YNX Chain Law.
- BFT compatibility also exposes `GET /ai/permissions` and `GET /ai/audit`. BFT mutation responses carry signer, committed transaction hash, block height, block-time timestamps, and record audit hashes. The BFT Gateway verifies Comet hash, block membership, action type, signer, nonce, record ID, and final ABCI state before returning success.
- Remote public proof must show independent gateway health and release identity, authenticated SSE with request/session evidence, a sensitive action proposal remaining non-executable until a matching scoped permission approval, then active permission audit, approved executable state, and audited gateway lookup/list on public endpoints.
