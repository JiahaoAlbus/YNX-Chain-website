# App

This directory mirrors the required website application boundary. The current implementation uses Vite under `src/`, and this directory documents the future Next.js route mapping if the website migrates to an app router.
