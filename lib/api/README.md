# API

The active API client is `src/lib/api/ynxApi.js`.

Website dynamic data must come from YNX Chain public APIs. The website must not hardcode block height, transaction count, validator count, TPS, TVL, users, exchange listings, stablecoin integrations, or partnerships.
